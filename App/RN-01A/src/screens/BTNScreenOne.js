import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const BTNScreenOne = ({ navigation }) => {
    return (
        <View>
            <Text>Bottom Tab Screen ONE</Text>
        </View>
    );
};

export default BTNScreenOne;

const styles = StyleSheet.create({});
